# Caddyfile
{
    local_certs
}

# Enforce HTTPS for the proxy
guardian.local:11435 {
    reverse_proxy localhost:11434
    tls internal
}

# Force redirect for anyone trying to hit the GUI on HTTP (Optional but recommended)
:5173 {
    redir https://{host}{uri}
}
